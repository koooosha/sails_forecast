[tool.poetry]
name = "rewe-challenge"
version = "0.1.0"
description = "this is time series analysis project"
authors = ["koosha <koosha512@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "3.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
